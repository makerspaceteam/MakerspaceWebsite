# Base URL of the backend API (Node/Express server in ../backend).
# Falls back to http://localhost:4000 if unset — see src/lib/api/client.js.
# VITE_BACKEND_URL=http://localhost:4000
VITE_BACKEND_URL=https://makerspace.idri.edu.kh